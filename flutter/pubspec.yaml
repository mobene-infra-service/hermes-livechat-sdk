name: hermes_livechat
description: Hermes LiveChat App SDK for Flutter (P0 draft). Provides basic chat UI, REST, and Centrifugo realtime client for visitor-side chat.
version: 0.1.0-draft
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  centrifuge: ^0.19.0
  http: ^1.2.0
  flutter_secure_storage: ^9.2.2
  uuid: ^4.4.0
  meta: ^1.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:

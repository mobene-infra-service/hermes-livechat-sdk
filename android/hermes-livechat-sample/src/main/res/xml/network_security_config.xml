<?xml version="1.0" encoding="utf-8"?>
<!--
  Sample Network Security Config: HTTPS only by default, with explicit cleartext
  exceptions for local development hosts. Production apps should follow the
  same pattern and avoid the global `android:usesCleartextTraffic="true"` flag.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
